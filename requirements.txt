# Core dependencies for MVP 0
# Audio processing
pydub==0.25.1
librosa==0.10.1

# Numerical and scientific computing
numpy==1.26.4
scipy==1.11.4
pandas==2.1.4

# Visualization
matplotlib==3.8.2

# Speech recognition for filler detection and WPM
faster-whisper==1.0.3

# Audio processing backend (requires ffmpeg at system level)
# Note: ffmpeg must be installed separately via system package manager
